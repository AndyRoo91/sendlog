# Screenshots

Captured 2026-07-11 against the merged Wave 4 build (grungy-chrome print pass)
at 430px width, via Playwright/Firefox with a seeded demo account. The main
[README](../../README.md) embeds these; recapture with the same filenames after
any big visual change:

| File | Screen |
|---|---|
| `dashboard.png` | Dashboard — Crag the buddy, weekly-goal rings, the training-plan card, stats |
| `logging.png` | Quick-log (TickSheet) — grade picker, style tray, the session feed |
| `progress.png` | Progress — charts, contribution heatmap, send pyramids |
| `feed.png` | Shared activity feed with reactions |
| `gyms.png` | Gyms — walls, sets, and colour circuits |

The `wave3-critique/` and `wave4-critique/` folders hold the full-app sets
taken for the style-critique rounds (12 screens each, 390px width).
